# ── Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.eslintcache

# ── Dependencies / builds / caches
node_modules/
dist/
dist-ssr/
.vite/
.cache/
.tmp/
temp/
tmp/
*.local

# ── Editors & OS junk
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.sw?
*.suo
*.ntvs*
*.njsproj
*.sln

# ── Environment (keep examples in repo)
.env
.env.*
!.env.example
!.env.sample

# ── Package manager auth tokens
.npmrc
.yarnrc
.pnpmrc

# ── Certificates & private keys
*.pem
*.key
*.crt
*.p12
*.pfx
*.cer

# ── Secrets & credentials
secrets/
.secret
*.secret

# ── SSH keys
.ssh/
id_rsa
id_dsa
id_ecdsa
id_ed25519

# ── Hosting provider local configs
.vercel/

# ── Tests & coverage (add if you use them)
coverage/
cypress/
playwright-report/
test-results/
jest/
__tests__/
*.spec.*
*.test.*

# ── Storybook (if used)
.storybook-out/
storybook-static/

# ── Monorepo/Build systems (optional)
.turbo/
nx-out/
